const initialState = {
  signUp: false,
  signIn: false,
};

const modal = (state = initialState, action) => {
  switch (action.type) {
    case '':

      break;

    default:
      return state;
  }
}

export default modal;
