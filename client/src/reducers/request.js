const initialState = {
  loading: false,
  error: false,
  msg: '',
};

const request = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default request;
